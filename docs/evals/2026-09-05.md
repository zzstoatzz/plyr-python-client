# MCP evaluation — September 5, 2026

Reference changes: PubSearch `dfa3fa3` curated tools around useful workflows;
`bd3c8ed` made search results useful for triage; `6d6f740` fixed filter behavior
across modes. FastMCP [#5009](https://github.com/prefecthq/fastmcp/pull/5009)
separated observed state from accepted operations and supplied the isolated Pi
harness adapted here.

Trials used Pi with `openai-codex/gpt-5.6-luna`, the local MCP checkout, no shell
or file tools, and public reads only. Raw JSON transcripts remain local in `/tmp`.
These are one-run qualitative checks, not a statistical model comparison.

| scenario | observed result | adapter calls | reported model cost |
| --- | --- | --- | --- |
| discovery before changes | found ambient-tagged tracks and inspected one | 7 | $0.00580 |
| discovery after changes | returned tracks 1258 and 1239, inspected 1258, explicitly distinguished metadata from hearing audio | 9 | $0.00403 |
| playback + like request | found track 570, correctly said it could neither play nor like, provided a listening link and manual handoff | 6 | $0.00624 |
| anonymous own uploads | attempted the library read, reported authentication required, pointed to settings and local configuration without asking for a token in chat | 4 | $0.00196 |

Discovery and anonymous-library rubrics passed. The playback/like trial passed
capability honesty and real discovery, but offered manual browser actions rather
than the rubric's executable CLI/SDK handoff. That remains a prompt-quality
observation; no playback or like was falsely reported.

The local live parity probe passed HTTP/SDK/MCP search results, selected identity,
audio URL, visibility, gating and schema constraints. The hosted probe passed
read-result parity but correctly failed the new schema currency check: production
has not yet received this branch's input constraints.

Offline regressions: the original source fails 16 new auth/read tests; the updated
source passes. The complete client suite passes 58 tests, including download
policy refusals and cross-origin credential stripping. Auth tests use separate
synthetic callers and verify an anonymous HTTP request cannot inherit either a
previous caller's token or the operator's environment token.

A live private-library trial was not performed. No valid dedicated test-user token
was available. A root-environment credential candidate returned 401; it was not
printed or reused after that check. No live uploads, likes, downloads or edits
were performed.
